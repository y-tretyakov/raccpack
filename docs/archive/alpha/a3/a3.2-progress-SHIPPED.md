# A3.2 — Progress + thin CLI (SHIPPED)

**Статус:** CLOSED 2026-08-18 · PR #77  
**Не реализовывать заново.** A3.5 расширяет CLI.

---

## Что в коде

### Core
- `app/raid/progress.rs`: `plan_phases`, `overall_percent`, `emit_phase_event`  
- Одно completion-событие `OperationKind::Raid` на planned phase (enabled + move)  
- Disabled phase → no event; fail-fast → failed + SKIPPED_MESSAGE for rest  
- **Нет** start-события «Starting raid…»

### CLI
- `Commands::Raid { project, yes, dry_run }`  
- Commit iff `yes && !dry_run`  
- Passphrase only on Commit; DryRun placeholder  
- `CliProgress` if !json else `NullProgress`  
- Human: `→ {phase}: {message}` + Success/Failed  
- **Exit 0** on any `Ok(RaidResult)` including `success: false` ← change in **A3.5**

### Tests
- `raid_progress` 5, `cli_raid` 6 — green  

---

## A3.5 заберёт

- `--no-stash` / `--no-rinse` / `--no-pack` / `--min-risk` / `--keep-sources` / `--fail-fast`  
- exit 1 if `!success`  
- richer human summary  
- wiki `raid.md`
