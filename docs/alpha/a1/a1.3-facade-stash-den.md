# A1.3 — Facade `stash` + артефакты `den/secrets/…`

**Фаза:** A1 — Stash (age)  
**Зависимости:** A1.1, A1.2, M4.2 (`ensure_den`, naming), M2.3 (`AppContext`, `RunMode`, `ProgressSink`)  
**Параллельность:** Dev + Test

---

## 1. Цель

Публичный use-case **`stash`** по facade-документу:

- DryRun: посчитать список и **ожидаемый** путь `.age`, ничего не писать/не удалять;
- Commit: select → encrypt to staging → place under `den/secrets/yyyy/mm/` → optional remove sources;
- Progress events;
- `StashResult` без raw secrets.

---

## 2. Facade API (канон)

Из `raccpack-facade-and-den.md` §5 — **совпадает 1:1 с реализацией**:

```rust
pub struct StashOptions {
    pub target: PathBuf,
    pub only_files: Option<Vec<PathBuf>>,
    pub min_risk: SensitiveRisk,      // default High
    pub remove_sources: bool,         // only effective in Commit
    pub batch_id: Option<String>,
}

pub enum AgeIdentity {
    Passphrase(String),               // wrapped to Zeroizing immediately inside
    Recipients(Vec<String>),          // A1: return Error::Unsupported if used
}

pub struct StashResult {
    pub archive_path: PathBuf,        // absolute final (or expected in DryRun)
    pub files_archived: usize,
    pub bytes_archived: u64,
    pub removed_sources: usize,       // 0 in DryRun
    pub dry_run: bool,
    pub manifest: Vec<StashManifestEntry>,
}

pub struct StashManifestEntry {
    pub original_path: PathBuf,
    pub risk: SensitiveRisk,
    pub size_bytes: u64,
}

pub fn stash(
    ctx: &AppContext,
    opts: &StashOptions,
    identity: &AgeIdentity,
    progress: &mut dyn ProgressSink,
) -> Result<StashResult, Error>;
```

---

## 3. Модульность и файлы

```text
src/
  app/
    stash.rs              # NEW: public stash()
  den/
    secrets_place.rs      # NEW: place_secrets_archive (mirror place_pack)
    names.rs              # reuse slug/timestamp/short_id
    layout.rs             # ensure_den already creates secrets/
  secrets/
    stash_select.rs       # A1.2
    stash_batch.rs        # A1.2
    stash_remove.rs       # A1.2
  archive/
    age_vault.rs          # A1.1
```

### 3.1. den/secrets_place.rs

```rust
pub struct PlaceSecretsRequest {
    pub den_root: PathBuf,
    pub project_name: String,
    pub source_age: PathBuf,      // staging file
    pub timestamp: Option<String>,
}

pub struct PlaceSecretsResult {
    pub absolute_path: PathBuf,
    pub relative_path: PathBuf,   // secrets/yyyy/mm/{slug}__{ts}__secrets.age
    pub size_bytes: u64,
}

pub fn place_secrets_archive(req: &PlaceSecretsRequest) -> Result<PlaceSecretsResult, Error>;
```

Naming (facade §9.2):

```text
secrets/{yyyy}/{mm}/{project_slug}__{utc_timestamp}__secrets.age
```

---

## 4. Алгоритм `stash`

```text
1. progress: phase=stash, 0%, "selecting files"
2. identity: only Passphrase; empty → Error; wrap Zeroizing
3. select_files_for_stash(...)
4. if empty → Error (or Ok with 0 files? → Error "no files matched min_risk")
5. slug/ts from target name + now (or batch_id in name if provided)
6. expected final path under ctx.paths.den_dir

7. if DryRun:
     return StashResult {
       archive_path: expected,
       files_archived: entries.len(),
       bytes_archived: sum sizes,
       removed_sources: 0,
       dry_run: true,
       manifest: from entries,
     }

8. ensure_den(den)
9. progress: "encrypting"
10. staging = den/staging/{short_id}/secrets.age
11. write_stash_age(entries, staging, passphrase)
12. progress: "placing"
13. placed = place_secrets_archive(...)
14. removed = 0
15. if opts.remove_sources:
      progress: "removing sources"
      removed = remove_stash_sources(...)
16. zeroize passphrase (Drop)
17. progress: 100% complete
18. Ok(StashResult { archive_path: placed.absolute, ..., removed_sources: removed, dry_run: false })
```

**Fail-safe:** if encrypt fails → no remove. If place fails → leave staging for debug (or cleanup); no remove. If remove fails mid-way → return Error but age already placed (document partial state).

---

## 5. Progress

`OperationKind::Stash`

| % | message |
|---|---------|
| 0 | Selecting sensitive files… |
| 30 | Encrypting archive… |
| 70 | Saving to den… |
| 90 | Removing sources… (if enabled) |
| 100 | Done |

No paths that embed secret **values**; paths to files OK.

---

## 6. Тесты

1. DryRun: den/secrets empty; manifest non-empty for fixture `.env`; removed_sources=0.
2. Commit: `.age` exists under secrets/yyyy/mm/; decrypt+tar contains file (test feature).
3. remove_sources true: source `.env` gone after success.
4. remove_sources false: source remains.
5. min_risk filters.
6. Recipients identity → Error unsupported.
7. Empty passphrase → Error.
8. JSON serde StashResult — no raw secret content from fixture.
9. Progress complete emitted.

```bash
cargo test -p raccpack-core stash
```

---

## 7. Критерий готовности

- [x] `stash` signature matches facade.
- [x] DryRun / Commit semantics.
- [x] Artifacts under `den/secrets/…/…__secrets.age`.
- [x] Zeroize path for passphrase.
- [x] Modules split as §3.
- [x] **F-PATH-1/3:** staging не внутри project; place только under den_root.
- [x] Tests green.

---

## 8. Отчёт

```markdown
## A1.3 — facade stash
### Сделано
- app/stash.rs
- den/secrets_place.rs
- DryRun/Commit + remove_sources
```

**Статус: CLOSED** (2026-08-15) — см. WORKLOG.

#### Реализовано
- `app/stash.rs` (created): `StashOptions`, `AgeIdentity` (`Passphrase(Zeroizing<String>)` / `Recipients`), `StashResult` (serde), `stash()` facade. DryRun считает список + ожидаемый путь, ничего не пишет/не удаляет; Commit: select → `write_stash_age` в `den/staging/{short_id}/secrets.age` → `place_secrets_archive_ensured` → (если `remove_sources`) `remove_stash_sources`.
- `den/secrets_place.rs` (created): `PlaceSecretsRequest` (+`batch_id`), `PlaceSecretsResult`, `place_secrets_archive` / `place_secrets_archive_ensured` (зеркало `place_pack`, `0600`, escaping-guard, cross-device fallback).
- `den/names.rs`: `secrets_relative_path` / `secrets_relative_path_token` (`secrets/{yyyy}/{mm}/{slug}__{ts}__secrets.age`).
- `domain/error.rs`: `Error::Unsupported { feature }` (+ suggestion) для `AgeIdentity::Recipients` и пустого passphrase → `Error::Encrypt`.
- F-PATH-1: containment в `stash_select` (A1.2); F-PATH-3: staging только под `den/staging/`, runtime-guard против den внутри project.
- `lib.rs` re-exports: `stash`, `StashOptions`, `StashResult`, `AgeIdentity`, `place_secrets_archive`, `PlaceSecretsRequest`, `PlaceSecretsResult`, `secrets_relative_path`.

#### Отклонения от спеки
- `AgeIdentity::Passphrase(Zeroizing<String>)` вместо `Passphrase(String)` — нулевая копия plaintext `String` не создаётся (инвариант zeroize A1.1; соответствует потоку CLI A1.4, где passphrase приходит уже `Zeroizing`).

#### Тесты
- `cargo test --workspace` → pass.
- `cargo test -p raccpack-core --features age-decrypt` → pass (roundtrip decrypt+untar).
- `tests/stash_facade.rs` (17): DryRun ничего не пишет (+ не создаёт den), Commit кладёт `.age` под `secrets/yyyy/mm/` (magic header, roundtrip), remove_sources on/off, min_risk фильтр, Recipients → Unsupported, пустой passphrase → Encrypt, serde StashResult без raw, progress 0/30/70/[90]/100, staging чист после Commit, den внутри project rejected, batch_id в имени.
- `cargo clippy --workspace --all-targets -- -D warnings` → clean; `cargo fmt --all -- --check` → clean.

#### Риски / follow-up
- Перезапись при коллизии имени в ту же секунду: `place_secrets_archive` пишет атомарно с overwrite (как `place_pack` документирует); уникальность/коллизии для raid решаются на A3.
- `batch_id` — name token вместо timestamp (`{slug}__{batch_id}__secrets.age`), `yyyy/mm` по-прежнему от now.
```
