# A3.1 — facade `raid` (SHIPPED, fail-fast)

**Статус:** CLOSED 2026-08-18 · PR #75 / #76  
**Не реализовывать заново.** База для A3.3 atomic upgrade.

---

## Что в коде

- `app/raid/` (после A3.2 split): `raid()`, `RaidOptions`, `RaidResult`, phase opts  
- Порядок: **stash → rinse → pack → move**  
- Phase `Err` → `Ok(RaidResult { success: false })`, later stages skipped («not run due to prior failure»)  
- Preconditions (empty project, missing identity, Recipients+stash on) → `Err`  
- DryRun: no den writes  
- **`Error::StashEmpty`:** ok_stage «nothing to stash», run continues (не fail-fast)  
- Identity ignored when `!stash.enabled`  
- Default: all phases on, `min_risk: High`, `remove_sources: true`, `deny_content_secrets: true`  
- Artifacts on partial success **остаются** (нет rollback)

---

## Тесты (регрессия — не ломать)

- `cargo test -p raccpack-core --test raid` (15+)  
- DryRun / Commit / fail-fast skip / stash disabled / pack-only / StashEmpty  

---

## Что НЕ входит (A3.3+)

- WAL, staging/{raid_id}, rollback, `rolled_back`, OrchestrationMode::Atomic  
- Manifest write  
- Full CLI toggles  

---

## Follow-up в A3.3

При atomic: destructive `remove_sources` / rinse commit-phase предпочтительнее mid-raid irreversible deletes (см. A3.3 policy).
