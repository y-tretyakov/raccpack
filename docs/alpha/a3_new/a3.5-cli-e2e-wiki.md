# A3.5 — CLI полный + E2E + orphan green + wiki

**Зависимости:** A3.3, A3.4  
**Бывший** WORKLOG «A3.4 CLI + E2E» — расширен.

---

## 1. Цель

Довести `racc raid` до Alpha exit:

1. Phase toggles и stash/pack flags  
2. `--fail-fast` → OrchestrationMode::FailFast  
3. **Exit 1** если `!success` (смена контракта A3.2)  
4. Human summary артефактов / rolled_back  
5. E2E fixture + orphan green  
6. Wiki `raid.md` + cli-usage + roadmap checkbox  

---

## 2. CLI flags (целевые)

```text
racc raid --project <PATH> --den <PATH>
          [--yes] [--dry-run] [--json]
          [--no-stash] [--no-rinse] [--no-pack]
          [--min-risk low|medium|high|critical]
          [--keep-sources]          # disable remove_sources
          [--no-content-deny]
          [--fail-fast]             # OrchestrationMode::FailFast
```

Passphrase: как stash (env / prompt) если stash enabled && Commit.

---

## 3. Примеры команд CLI

```bash
# Dry-run (как сейчас)
racc raid --project ~/DEV/PROJS/my-api --den ~/.raccpack/den

# Atomic commit (default)
export RACCPACK_PASSPHRASE='…'
racc raid --project ~/DEV/PROJS/my-api --den ~/.raccpack/den --yes

# JSON + проверка rollback fields
racc raid --project ~/DEV/PROJS/my-api --den ~/.raccpack/den --yes --json \
  | jq '{success, rolled_back, stages}'

# Без stash
racc raid --project ~/DEV/PROJS/my-api --den ~/.raccpack/den --yes --no-stash

# Debug fail-fast (orphan возможен)
racc raid --project ~/DEV/PROJS/my-api --den ~/.raccpack/den --yes --fail-fast

# Keep sources
racc raid --project ~/DEV/PROJS/my-api --den ~/.raccpack/den --yes --keep-sources

# Exit code: success=false → 1
racc raid --project /bad --den /tmp/den --yes ; echo $?
```

---

## 4. Exit codes (смена A3.2)

| Code | When |
|------|------|
| 0 | `Ok` && `success == true` |
| 1 | `Err` **или** `Ok` && `success == false` (вкл. rolled_back failure) |

Обновить module-doc `commands/raid.rs` и тесты `cli_raid`.

---

## 5. E2E checklist

```bash
DEN=/tmp/racc-e2e-den; PROJ=fixture; rm -rf "$DEN"
RACCPACK_PASSPHRASE=test \
  cargo run -p raccpack-cli -- raid --project "$PROJ" --den "$DEN" --yes
test -f "$DEN"/.den-version
find "$DEN/secrets" -name '*.age' | grep .
find "$DEN/packs" -name '*.tar.zst' | grep .
find "$DEN/manifests" -name '*.json' | grep .
# sources removed if default remove_sources
# node_modules gone if rinse on
```

Orphan: inject failure → no den artifacts; `rolled_back` in JSON.

---

## 6. Wiki / docs (теперь можно)

- `wiki/raid.md` (atomic, flags, examples)  
- `cli-usage.md` — raid Available  
- roadmap checkbox  

Согласовано с решением WORKLOG A3.1/A3.2: wiki не трогали до зелёного полного CLI — **A3.5 закрывает**.

Спека-артефакт: обновить [wiki-raid.md](wiki-raid.md) в этом репо спек (уже atomic-oriented).

---

## 7. DoD

- [ ] Все flags выше.  
- [ ] Exit 1 on !success.  
- [ ] E2E + orphan green.  
- [ ] Wiki + cli-usage synced.  
- [ ] Alpha raid exit criteria met → можно A4.

---

## 8. Отчёт

```markdown
## A3.5 — CLI E2E wiki
### exit on !success
1
### wiki
updated
### alpha raid
exit criteria pass
```
