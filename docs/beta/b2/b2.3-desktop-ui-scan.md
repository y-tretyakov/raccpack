# B2.3 — UI: root/den, projects, secrets

**Зависимости:** B2.2

---

## 1. Цель

- Settings: scan_root, den_dir
- Projects table (sniff)
- Secrets list (dig): risk badge, masked, path, git_status

Stores: `useScanStore`, `useSecretsStore`, `useSettingsStore` — **masked only**.

---

## 2. Модульность

```text
apps/desktop-ui/src/
  stores/{scan,secrets,settings}.ts
  pages/{Projects,Secrets,Settings}.tsx
  components/{DataTable,RiskBadge}.tsx
```

---

## 3. DoD

- [ ] Set root → sniff → dig E2E in UI
- [ ] Masked in DOM
- [ ] Error + suggestion shown
