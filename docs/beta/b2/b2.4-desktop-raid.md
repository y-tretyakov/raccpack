# B2.4 — Desktop raid (Atomic)

**Зависимости:** B2.3, core atomic raid

---

## 1. Цель

- Dry-run preview → confirm → passphrase dialog → progress events → result
- Passphrase **not** in Zustand persist
- Show `rolled_back` / stages

---

## 2. Flow

```text
Raid button → dry-run invoke → Confirm Commit
  → PassphraseDialog (if stash)
  → invoke raid; listen raid-progress
  → RaidResult panel
  → clear passphrase
```

---

## 3. DoD

- [ ] raid through core only
- [ ] Progress updates UI
- [ ] No passphrase in localStorage
- [ ] DryRun default before Commit
