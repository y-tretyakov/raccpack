# B2.5 — Desktop ephemeral reveal IPC

**Зависимости:** B2.3, **B3.4** EphemeralSecret

---

## 1. Цель

```rust
#[tauri::command]
fn reveal_secret_ephemeral(path: String, finding_id: String) -> Result<String, String>;
```

- Raw goes to **isolated** React component (local state only), **not** Zustand.
- On unmount: drop; Rust zeroize buffer.

---

## 2. DoD

- [ ] Isolated component path
- [ ] No localStorage of raw
- [ ] Threat test: store dump has no raw
