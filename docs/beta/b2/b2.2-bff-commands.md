# B2.2 — BFF: sniff / dig / raid (DTO only)

**Зависимости:** B2.1

---

## 1. Цель

Tauri commands map to core; errors via Display+suggestion; **masked DTO only**.

```rust
#[tauri::command]
fn sniff(root: String, force_refresh: bool) -> Result<SniffResult, String>;

#[tauri::command]
fn dig(project: Option<String>, scan_content: bool) -> Result<DigResult, String>;

#[tauri::command]
fn raid(/* opts */) -> Result<RaidResult, String>;
```

Progress: `app.emit("raid-progress", …)`.

---

## 2. Модульность

```text
crates/raccpack-tauri/src/commands/{mod,sniff,dig,raid}.rs
error_map.rs
```

---

## 3. DoD

- [ ] Real core results
- [ ] No raw secret fields in responses
- [ ] Path validation
