# B4.3 — User docs + install notes

---

## 1. Цель

1. Quickstart, CLI reference, TUI, Desktop, den, security, atomicity, reveal
2. Install notes: cargo install / binaries / desktop
3. EN wiki at least quickstart (F-DOC-1 partial)
4. **Flags match --help** (no docs for unimplemented)

---

## 2. Install examples (must be accurate)

```bash
cargo install --path crates/raccpack-cli
cargo install --path crates/raccpack-tui
cargo build --release -p raccpack-cli -p raccpack-tui
cargo tauri build   # desktop artifacts path documented
```

---

## 3. DoD

- [ ] Quickstart reproducible notes
- [ ] Flag tables match implementation
- [ ] Security page links threat checklist
- [ ] Atomic + reveal documented
