# B4.2 — Parallel sniff (`parallel_jobs`)

---

## 1. Цель

```toml
[advanced]
parallel_jobs = 4
```

```bash
racc sniff --root PATH --jobs 8
racc sniff --root PATH --jobs 1   # deterministic debug
```

- rayon pool; jobs=1 sequential
- jobs=0 → Error (or clamp — **зафиксировать Error**)
- Result ordering deterministic (sort paths)

---

## 2. DoD

- [ ] Config + CLI override
- [ ] Same project set jobs=1 vs 4
- [ ] follow_links still false
