# B1.3 — Read-only workspace: Overview, Projects, Findings, Den

**TUI-spec:** Phase 2  
**Core:** load config, optional cached sniff data — **no auto destructive ops on start**

---

## 1. Цель

### Overview
Workspace health: counts (projects/findings/den), pipeline attention, recent ops (empty ok), cache age if known.

### Projects
Virtualized table: name, stack (flat; tree later if Detect), size, findings count, risk, git.  
Filter `/`, sort. Detail split/overlay — not a separate route stack.

### Findings
Renamed from «Secrets»: risk, project, location, type, **masked** only.  
Never raw.

### Den
Tabs: Packs | Secrets | Manifests — metadata only, no decrypt.

---

## 2. Data

- Prefer types from core: `Project`, `ScanReport`, `SensitiveFinding`, …
- View models only for aggregation.
- Startup: load `RaccConfig` + `TuiPreferences`; missing config → `InitWizard` screen (minimal in B1.3 or stub → full in polish).

Default: **do not** auto-run full sniff on start (setting later).

---

## 3. Safety

- READ only in this stage (no stash/rinse/raid triggers required).
- Empty states with action hints (`[Enter] Run sniff` → wires in B1.4).

---

## 4. Tests

- Snapshot: Overview/Projects/Findings/Den empty + populated fixtures.
- Virtualization: large list doesn't build 10k widgets.
- No secret plaintext in buffer tests (fixture with fake secret string).

---

## 5. DoD

- [ ] Four views navigable
- [ ] Masked findings only
- [ ] 80×24 usable
- [ ] Snapshots green
