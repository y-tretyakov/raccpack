# B1.7 — Polish: palette, i18n, themes, mouse, reveal hook

**TUI-spec:** Phase 6  
**Reveal:** depends on **B3.4** EphemeralSecret — if B3.4 not done, ship masked-only and stub “reveal unavailable”.

---

## 1. Цель

- Command palette `:` (fuzzy): sniff, dig, raid project=…, theme, quit.
- i18n keys ru/en (centralized strings).
- Extra themes P1/P2; Nocturnal remains default.
- Mouse optional; all actions keyboard-reachable.
- P1 detach: leave operation detail while job runs (still one destructive job).
- Reveal: `v` on finding → confirm → ephemeral modal → wipe (B3.4 API).
- Init wizard if config missing.
- Docs: `docs/tui.md`, keymap; update [wiki-tui.md](wiki-tui.md).
- Perf pass: targets from TUI-spec §57.

---

## 2. Out of scope (still)

Animations-heavy UX, background job queue P2, raw secret view, auto-run ops on start.

---

## 3. DoD

- [ ] Palette works for primary ops
- [ ] i18n structure in place (ru+en minimum)
- [ ] Reveal only via EphemeralSecret or disabled
- [ ] Wiki TUI matches keys
- [ ] B1 exit: production-ready checklist TUI-spec §75 / v4 §14 subset
