# B1.2 — Shell: header, nav, status, log, layout, help

**TUI-spec:** Phase 1

---

## 1. Цель

Операционный chrome (ещё без live core data):

```text
Header:  RACC · workspace path · status READY
Sidebar: Overview | Projects | Findings | Den | Operations | Config
Main:    placeholder
Op bar:  (empty / last status)
Log:     3–5 lines ring buffer
Status:  q quit · ? help · : commands (palette later)
```

- Responsive breakpoints (≥140 … <80) из TUI-spec §28 / v4 §10.
- Focus: NavBar | Main | Log | Overlay.
- `?` help overlay; `q` / Ctrl+C quit.
- Theme **Nocturnal** semantic colors; `NO_COLOR` safe.

---

## 2. Модульность

```text
src/layout.rs
src/views/{overview,projects,...}_stub.rs  # empty ok
src/widgets/{header,sidebar,footer,log_view,help}.rs
src/keymap.rs
```

---

## 3. Keys (P0)

| Key | Action |
|-----|--------|
| `1`–`6` / sidebar | change ViewId |
| `Tab` / `S-Tab` | focus cycle |
| `?` | help |
| `q` / `Ctrl+C` | quit |
| `l` | toggle log expand |

---

## 4. DoD

- [ ] Shell renders 80×24 without panic
- [ ] Resize handled
- [ ] Help lists keys
- [ ] Snapshot tests: shell empty + help open
