# B1.4 — TUI raid (Atomic) + progress

**Зависимости:** B1.3, core `raid` Atomic

---

## 1. Цель

- Dry-run preview → confirm → passphrase if stash → progress → result.
- Reflect `rolled_back` / stages.
- Default Atomic (no FailFast in TUI unless debug key).

---

## 2. Flow

```text
Select project → R
  → Dry-run summary
  → Confirm [y/N]
  → Passphrase modal (if stash)
  → Progress phases (incl. commit)
  → Result (success / rolled_back)
```

Keys: `y`/`n`, `K` keep-sources, `S` skip-stash.

---

## 3. DoD

- [ ] raid() only through core
- [ ] Passphrase zeroized after use
- [ ] Progress + result screens
- [ ] DryRun never writes
