# B3.4 — Контракт EphemeralSecret

**Критично до B1.5 / B2.5 / B3.5**

---

## 1. Цель

```rust
/// NOT Serialize. Never in DigResult/RaidResult.
pub struct EphemeralSecret {
    inner: Zeroizing<String>,
}

impl EphemeralSecret {
    pub fn expose(&self) -> &str;
}

pub fn reveal_finding(
    path: &Path,
    finding_ref: &FindingRef,
) -> Result<EphemeralSecret, Error>;
```

Opt-in only. Not in JSON reports. CI typically never calls.

---

## 2. DoD

- [ ] Cannot accidentally serde into reports
- [ ] Drop/zeroize
- [ ] API documented for CLI/TUI/Desktop
