# B3.6 — Threat checklist + no-leak tests

**Зависимости:** B3.1–B3.5, A4.3 tracing

---

## 1. Checklist

| # | Threat | Mitigation | Test |
|---|--------|------------|------|
| 1 | Secret in log | redaction | grep -vv |
| 2 | Secret in Error | path-only messages | unit |
| 3 | Secret in JSON | DTO masked | serde assert |
| 4 | Symlink escape | containment | unit |
| 5 | Pack includes .env | name+content deny | integration |
| 6 | Passphrase in Zustand | B2 design | review + no persist |
| 7 | Reveal in store | B1.5/B2.5 | no raw in state |
| 8 | Den world-readable | 0700/0600 | unix |

---

## 2. Примеры ручной проверки

```bash
SECRET_VAL='unique-leak-token-b3'
echo "API_KEY=$SECRET_VAL" > /tmp/proj/.env
RACCPACK_PASSPHRASE='unique-pass-b3' \
  racc stash --project /tmp/proj --den /tmp/den --yes -vv 2>&1 | tee /tmp/out.log
grep -F "$SECRET_VAL" /tmp/out.log && echo FAIL || echo OK
grep -F 'unique-pass-b3' /tmp/out.log && echo FAIL || echo OK
racc dig --project /tmp/proj --json | grep -F "$SECRET_VAL" && echo FAIL || echo OK
```

---

## 3. DoD

- [ ] Checklist in docs
- [ ] Automated redaction tests green
- [ ] Manual checks in wiki-security
