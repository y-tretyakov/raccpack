# B3.2 — EnabledGroups type-safe

**Follow-up:** F-SEC-2

---

## 1. Цель

```rust
pub struct EnabledGroups { /* bitflags or enumset */ }
// AppContext.secret_groups_override: Option<EnabledGroups>
```

Config:

```toml
[sensitive]
groups = ["env", "keys", "cloud"]
```

CLI (optional):

```bash
racc dig --project PATH --groups env,keys
racc dig --project PATH --groups all
```

dig/stash/pack honor groups. Fingerprint stable for repeated secrets.

---

## 2. DoD

- [ ] EnabledGroups public API
- [ ] Config + dig filter
- [ ] No raw in repeated DTO
- [ ] Tests green
