# B3.1 — Content-deny + единый name-policy + default_pack

**Follow-up:** F-SKIP-1, F-SEC partial

---

## 1. Цель

1. Pack default `deny_content_secrets: true` everywhere.
2. Single name-deny source: `secrets::match_filename` (no second table).
3. `SkipPolicy::default_pack()` — `.next`, `coverage`, `.turbo`, …
4. Optional tune noisy content markers.

---

## 2. Модульность

```text
scan/skip.rs          # default_pack()
archive/pack.rs       # use it
secrets/content.rs    # optional tweaks
```

---

## 3. Тесты

- Pack excludes content Critical even if name clean
- default_pack skips `.next`
- Name deny still excludes `.env`

---

## 4. DoD

- [ ] default_pack used by pack defaults
- [ ] content deny default true
- [ ] Single name-policy source
- [ ] Tests green
