# B3.3 — Path containment + den 0700/0600

**Follow-up:** F-PATH-1 (добить везде)

---

## 1. Цель

```rust
pub fn is_under_root(root: &Path, child: &Path) -> bool;
pub fn ensure_under_root(root: &Path, child: &Path) -> Result<(), Error>;
```

Wire: dig only_files, stash, pack, rinse, raid, den place.  
Symlink-out → not under root.  
Den `0700`, files `0600` best-effort Unix.

---

## 2. Модульность

```text
src/fsutil/contain.rs
den/layout.rs place.rs
```

---

## 3. DoD

- [ ] Helper wired in all write/select paths
- [ ] Symlink escape tests
- [ ] Perms best-effort tests (unix)
