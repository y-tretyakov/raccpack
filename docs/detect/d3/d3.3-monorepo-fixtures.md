# D3.3 — Фикстуры монорепо + Detect v2 exit gate

**Зависимости:** D3.1, D3.2

---

## 1. Цель

Закрыть **Detect v2 exit criteria** фикстурами и CI-тестами.

### Fixtures (в repo)

```text
fixtures/detect_v2/
  mono-rust-node/
    Cargo.toml
    src/main.rs
    target/.keep          # or create in test
    web/
      package.json
      node_modules/x/
  mono-python-js/         # optional
    pyproject.toml
    frontend/package.json
  simple-rust/            # regression priority_table
    Cargo.toml
```

---

## 2. Test matrix

| Test | Mode | Assert |
|------|------|--------|
| sniff tree | composite_dag | both ecosystems in stack_tree |
| sniff flat | priority_table | stack filled; stack_tree None |
| rinse scopes | composite_dag | node_modules under web gone; rust target under backend |
| rinse classic | priority_table | existing clean tests |
| simple project | both modes | no panic; sensible stack |

---

## 3. CI

```bash
cargo test -p raccpack-core detect
cargo test -p raccpack-core --test rinse
cargo test -p raccpack-cli sniff
cargo test --workspace
```

---

## 4. Detect v2 exit checklist

- [ ] Monorepo sniff shows correct tree
- [ ] rinse removes only relevant trash in dag mode
- [ ] priority_table: no regression
- [ ] CLI `--detect-mode` works
- [ ] Docs/wiki sniff + configuration updated
- [ ] WORKLOG: Detect v2 0.4.x closed → Beta unblocked for DAG UI

---

## 5. Out of scope (later)

- Full manifest-deps parsing (next/react from package.json) — optional enhancement, was deferred from M2.2
- Flat `detect/frameworks/` module split — only when 4–5+ rules per ecosystem
- Case-insensitive FS policy (macOS) — RC
