# D2.3 — Обратная совместимость: flat `stack` + `stack_tree`

**Зависимости:** D2.2

---

## 1. Цель

| Поле | priority_table | composite_dag |
|------|----------------|---------------|
| `stack` (String/Stack summary) | always | always |
| `stack_tree` | `None` (or omit) | `Some(…)` |

Старые JSON-скрипты, читающие только `stack` / language, **не ломаются**.

Human sniff: в dag mode показать indent tree или вторую колонку; priority_table — как сейчас.

---

## 2. schema

Additive field only. Если bump `ScanReport.schema_version` — только при breaking; предпочтительно **не** bump для additive `stack_tree`.

---

## 3. Тесты

- priority_table: stack_tree is None; stack non-empty for known projects
- composite_dag: both set; stack summary non-empty
- serde: old fixture without stack_tree still deserializes if needed

---

## 4. DoD

- [ ] Compat table above holds
- [ ] Sniff JSON documented
- [ ] Tests green
