# D2.2 — Разрешение конфликтов / merge

**Зависимости:** D2.1

---

## 1. Цель

Правила (зафиксировать в rustdoc):

1. **Scope nesting:** более глубокий scope — child; rinse target = scope path.
2. **Confidence:** низкий confidence можно отфильтровать порогом (default 0.0 = keep all; optional later).
3. **Frameworks:** union + dedup, stable order by registry.
4. **Same ecosystem twice** at same scope: merge frameworks, max confidence.
5. **Language at root:** summary for flat `stack` — priority table policy on root-level detections (compat).

Не «один победитель на весь monorepo».

---

## 2. Модульность

```text
src/detect/merge.rs
```

---

## 3. Тесты

- Nested frontend+backend both visible.
- Stable JSON ordering (sort children by scope path).
- Merge same-ecosystem frameworks dedup.

---

## 4. DoD

- [ ] Rules documented in rustdoc
- [ ] Deterministic tree
- [ ] Tests green
