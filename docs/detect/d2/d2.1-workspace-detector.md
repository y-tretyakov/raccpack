# D2.1 — `WorkspaceDetector`: опрос всех модулей → дерево

**Зависимости:** D1  
**Параллельность:** Dev + Test

---

## 1. Цель

Не выбирать «одного победителя». Собрать Detection по поддеревьям.

```rust
pub struct WorkspaceDetector { /* holds registry */ }

impl WorkspaceDetector {
    pub fn detect_tree(
        &self,
        project_root: &Path,
        markers_by_path: &[(PathBuf, Vec<MarkerHit>)], // or re-scan
    ) -> Result<StackNode, Error>;
}
```

### Алгоритм (sketch)

1. Иметь candidates / dirs under project (reuse walk + markers; follow_links false).
2. Для каждого интересного scope path вызвать applicable detectors.
3. Построить узлы с `scope = path`.
4. Связать parent/child по path containment (child scope under parent scope).
5. Корень = project_root (может быть multi-ecosystem).

---

## 2. Модульность

```text
src/detect/
  workspace.rs       # WorkspaceDetector
  graph.rs           # optional: edge building
```

---

## 3. Тесты

- Fixture monorepo Rust root + `web/package.json` → both ecosystems in tree.
- Single Cargo.toml project → one primary node (tree still valid).
- follow_links false preserved.

```bash
cargo test -p raccpack-core workspace
```

---

## 4. DoD

- [ ] WorkspaceDetector returns StackNode
- [ ] composite_dag mode uses this path
- [ ] priority_table path untouched
- [ ] Tests green
