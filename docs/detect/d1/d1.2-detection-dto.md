# D1.2 — `Detection` / `StackNode` DTO

**Зависимости:** D1.1

---

## 1. Цель

```rust
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct Detection {
    pub ecosystem: String,           // "rust", "node", …
    pub language: Option<String>,
    pub frameworks: Vec<String>,
    pub confidence: f32,             // 0.0..=1.0
    pub scope: PathBuf,              // subtree root this detection applies to
    pub markers: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct StackNode {
    pub detection: Detection,
    pub children: Vec<StackNode>,
}
```

`Project` (additive):

```rust
pub stack: Stack,                    // existing flat summary — ALWAYS filled
pub stack_tree: Option<StackNode>,   // Some in composite_dag mode
```

`ScanReport.schema_version`: bump if JSON shape changes for consumers — document (recommend keep schema_version, additive field only).

---

## 2. Модульность

```text
src/detect/types.rs   # or domain/ if preferred — зафиксировать detect/
src/domain/project.rs # stack_tree: Option<StackNode>
```

---

## 3. Тесты

- Serde roundtrip Detection / StackNode
- confidence clamp documented
- Project with stack_tree None still serializes

---

## 4. DoD

- [ ] DTO public + Serialize
- [ ] Project.stack_tree additive
- [ ] Flat stack always present
- [ ] Tests green
