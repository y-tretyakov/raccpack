[package]
name = "raccpack-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core library for raccpack: scan, secrets, pack, den"
rust-version.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "1"
toml = "0.8"

[dev-dependencies]
serde_json = "1"
serial_test = "3"
tempfile = "3"
